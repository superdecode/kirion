{
  "buildCommand": "cd kirion/frontend && npm install && npm run build",
  "installCommand": "npm install --prefix api && npm install --prefix kirion/backend",
  "outputDirectory": "kirion/frontend/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" },
    { "source": "/((?!api).*)", "destination": "/index.html" }
  ]
}
